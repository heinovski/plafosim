---
kind: pipeline
type: docker
name: testing

steps:
- name: unit tests
  image: python
  commands:
    - pip install pytest
    - pip install pytest-cov
    - export PYTHONPATH=$(pwd)/src
    - pytest -v --cov=plafosim tests/unit_tests
