---
kind: pipeline
type: docker
name: default

steps:
- name: run pytest
  image: python
  commands:
    - pip install pytest
    - pip install pytest-cov
    - export PYTHONPATH=$(pwd)/src
    - pytest -v --cov=plafosim
